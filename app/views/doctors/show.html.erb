<h2><%= @doctor.name %> </h2>
<ul>
  <% @doctor.appointments.to_a.each do |appointment|%>
  <li>
    <%= link_to appointment.patient.name, patient_path(appointment.patient)%>
    <%=appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M %P")%>
  </li>

  <%end%>
</ul>
